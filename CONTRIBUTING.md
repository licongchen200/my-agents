# Contributing

Nothing is committed directly to `main`. All work happens on branches and
is merged in via pull request.

Specs live in Notion. This repository holds the worker code and the
deploy pipeline, not the specs themselves.

Secrets are provided by Infisical at runtime and must never be committed
to this repository.
